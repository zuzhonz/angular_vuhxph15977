import { GetGenderPipe } from './get-gender.pipe';

describe('GetGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new GetGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
